services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bluesome_container
    restart: unless-stopped
    shm_size: "2gb"
    volumes:
      - bluesome_home:/home/ubuntu

volumes:
  bluesome_home: