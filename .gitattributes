# Force LF regardless of the checking-out platform's git config (core.autocrlf),
# so shell scripts keep a working shebang when built from WSL/Linux.
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
